import { FC } from "react";
import NavBar from "../components/NavBar";
import App from "../App";

const Layout: FC = () => {
  return (
    <>
      <NavBar />
      <App />
    </>
  );
};

export default Layout;
